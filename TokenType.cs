namespace Lab_1___1156813
{
    public enum TokenType
    {
        plus = '+',
        minus = '-',
        mult = '*',
        div = '/',
        lParen = '(',
        rParen = ')',
        eof = (char)0,
        empty = (char)1,
        _null = (char)2,
        number = (char)3
    }
}
